{
  "$schema": "https://openclaw.ai/schemas/plugin.schema.json",
  "id": "devflow",
  "name": "DevFlow",
  "version": "0.1.0",
  "description": "Project context snapshot and proxy status for AI handoff",
  "configSchema": {
    "type": "object",
    "properties": {
      "projectPath": {
        "type": "string",
        "description": "Project directory (defaults to workspace or cwd)"
      }
    }
  }
}
